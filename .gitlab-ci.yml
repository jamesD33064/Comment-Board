image: docker-hub.program.com.tw/bikonnect-pluspay/php8.1-mongodb:dev

stages:
  - test

before_script:
  - apk add --no-cache openssh-client
  - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
  - chmod 600 ~/.ssh/id_rsa
  - eval $(ssh-agent -s)
  - ssh-add ~/.ssh/id_rsa


cs_test:
  stage: test
  tags:
    - docker
  before_script:
  - apt-get update
  - apt-get install -y ssh
  script:
    - php-cs-fixer fix app/

    - git config user.email "james.huang@program.com.tw"
    - git config user.name "james.huang"
    - git remote add cc https://ACCESS_TOKEN:$ACCESS_TOKEN@gitlab1.program.com.tw/BackendGroup/freshman/now/202307-james/comment.git
    - git add .
    - git commit -m "push back from pipeline"
    # - git checkout -b ci_test
    - git push cc ci_test -o ci.skip
